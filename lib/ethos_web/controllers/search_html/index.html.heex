<div class="mx-auto max-w-2xl px-4 py-10">
  <h1 class="font-display text-display-sm">Search</h1>
  <form action={~p"/search"} method="get" class="mt-4 flex gap-2">
    <input
      type="search"
      name="q"
      value={@q}
      placeholder="Towns, museums, restaurants…"
      class="w-full rounded-lg border-line"
      autofocus
    />
    <button class="rounded-lg bg-accent px-4 py-2 text-accent-ink">Search</button>
  </form>

  <p
    :if={@q != "" and @results.guides == [] and @results.places == []}
    class="mt-8 text-ink-muted"
  >
    No results for "{@q}".
  </p>

  <section :if={@results.guides != []} class="mt-8">
    <h2 class="font-semibold">Guides</h2>
    <ul class="mt-3 space-y-2">
      <li :for={g <- @results.guides}>
        <.link navigate={~p"/g/#{g.slug}"} class="underline">{g.title}</.link>
        <span class="text-sm text-ink-muted">· {g.destination}</span>
      </li>
    </ul>
  </section>

  <section :if={@results.places != []} class="mt-8">
    <h2 class="font-semibold">Places</h2>
    <ul class="mt-3 space-y-2">
      <li :for={p <- @results.places}>
        <.link navigate={~p"/p/#{p.slug}"} class="underline">{p.name}</.link>
        <span class="text-sm text-ink-muted">· {place_meta(p)}</span>
      </li>
    </ul>
  </section>
</div>
